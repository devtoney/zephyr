sample:
  name: SPI FRAM Module Sample
tests:
  sample.drivers.spi.spi_sam0:
    tags:
      - drivers
      - fram
    harness: fram
    depends_on:
      - gpio
      - spi
    filter: dt_alias_exists("spi-1")
